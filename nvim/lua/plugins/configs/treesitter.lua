local options = {
  ensure_installed = { "lua", "proto", "go", "gomod", "rust", "html", "javascript", "typescript", "css", "json", "toml", "yaml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
